namespace SPRENCIA.Domain.Models
{
    // Clase que representa la consulta a base de datos de la tabla activities_schedules + schedules (inner join).
    /* public class ActivitiesSchedulesSchedule
    {
        public ActivitiesSchedules ActivitySchedules { get; set; }
        public Schedule Schedules { get; set; }
    }
    */
}
